// This component has been replaced by SettingsModal.jsx
// Settings (Session Retention + Delete All) are now in a global modal
// accessible from the sidebar gear icon, not embedded per-site.
export default function SettingsPanel() {
  return null;
}
